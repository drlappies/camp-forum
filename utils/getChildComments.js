function getChildComments(parentComment, parentCommentArray) { // takes in a parent comment object from outer loop
    for (let k = 0; k < parentCommentArray.length; k++) {
        if (parentComment._id === parentCommentArray[k]._id) {
            
        }
    }
}